```mermaid
sequenceDiagram
    participant browser
    participant server

    Note right of browser: The browser adds new note to local variable 
    Note right of browser: The browser rerenders the part of web page that show the list of notes
    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa
    activate server
    deactivate server
```
